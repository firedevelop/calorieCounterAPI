export type Meal = {
    id: number;
    name: string;
    calories: number;
};

export type Category = {
    id: number;
    name: string;
};

export type Activity = {
    id: number;
    name: string;
    caloriesBurned: number;
};